# ============================================================================
# Docker 本地环境（Study 根目录）
# ============================================================================

# 本地环境变量：里面有 MySQL 密码 / JWT 密钥 / API Key / 邮箱授权码，**绝对不能提交**
.env

# 运行期数据：MySQL 数据目录、上传文件
data/

# JVM 崩溃日志等临时产物
hs_err_pid*.log
*.log
