[project]
name = "studykeeper"
version = "0.1.0"
description = "Add your description here"
authors = [
    { name = "Lunorin", email = "1601048781@qq.com" }
]
requires-python = ">=3.7"
dependencies = [
    "fastapi[standard]>=0.103.2",
]

[project.scripts]
studykeeper = "studykeeper:main"

[[tool.uv.index]]
url = "https://pypi.tuna.tsinghua.edu.cn/simple"
default = true

[build-system]
requires = ["uv_build>=0.12.13,<0.13.0"]
build-backend = "uv_build"
